{
  "slug": "examples/youtube-channel-email-scraper/update-settings",
  "title": "Update Settings",
  "description": "Configure YouTube Channel Email Scraper settings",
  "category": "YouTube Channel Email Scraper",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/squids/{squid_hash}",
    "requiresAuth": true
  },
  "content": {
    "introduction": "Configure your YouTube Channel Email Scraper squid settings. This scraper uses only the general settings with no scraper-specific parameters.",
    "sections": {
      "squidSettings": {
        "title": "Squid Settings",
        "content": "Configure general squid settings:",
        "columns": [
          {"header": "Setting", "key": "setting"},
          {"header": "Type", "key": "type"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"setting": "**name**", "type": "string", "description": "Display name for your squid configuration"},
          {"setting": "**concurrency**", "type": "integer", "description": "Number of parallel scraping threads (default: 1)"},
          {"setting": "**export_unique_results**", "type": "boolean", "description": "Export only unique results (deduplicated)"},
          {"setting": "**to_complete**", "type": "boolean", "description": "Run until all tasks complete"},
          {"setting": "**no_line_breaks**", "type": "boolean", "description": "Remove line breaks from results"}
        ]
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Request body format"
      }
    ],
    "proTips": [
      {
        "type": "note",
        "content": "This scraper has no scraper-specific parameters - only general squid settings are available."
      },
      {
        "type": "tip",
        "content": "Keep concurrency low (1-2) to avoid rate limiting from YouTube."
      }
    ]
  },
  "seo": {
    "title": "Update Settings - YouTube Channel Email Scraper - lobstr.io API",
    "description": "Configure YouTube Channel Email Scraper settings using the lobstr.io API."
  }
}

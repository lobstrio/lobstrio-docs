{
  "slug": "examples/sales-navigator-leads-scraper/add-tasks",
  "title": "Add Tasks",
  "description": "Add LinkedIn Sales Navigator search URLs to scrape lead profiles",
  "category": "Sales Navigator Leads Scraper",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/tasks",
    "requiresAuth": true
  },
  "content": {
    "introduction": "Add LinkedIn Sales Navigator search URLs to scrape lead profiles. This scraper requires a linked Sales Navigator account to access the data.\n\nCopy the search URL directly from Sales Navigator after applying your filters (company size, seniority level, industry, location, etc.). The scraper will paginate through the search results automatically.",
    "sections": {
      "requestBody": {
        "title": "Request Body",
        "columns": [
          {"header": "Field", "key": "field"},
          {"header": "Type", "key": "type"},
          {"header": "Required", "key": "required"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"field": "**squid**", "type": "string", "required": "Yes", "description": "Hash of your squid configured with the Sales Navigator Leads Scraper crawler"},
          {"field": "**tasks**", "type": "array", "required": "Yes", "description": "Array of task objects, each containing a `url` field"}
        ]
      },
      "taskObject": {
        "title": "Task Object",
        "columns": [
          {"header": "Field", "key": "field"},
          {"header": "Type", "key": "type"},
          {"header": "Required", "key": "required"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"field": "**url**", "type": "string", "required": "Yes", "description": "Sales Navigator people search URL (e.g., `https://www.linkedin.com/sales/search/people?query=...`)"}
        ]
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Request body format"
      }
    ],
    "proTips": [
      {
        "type": "tip",
        "content": "Apply filters in Sales Navigator first, then copy the URL. All filter parameters will be preserved."
      },
      {
        "type": "note",
        "content": "This scraper requires a linked Sales Navigator account. Ensure your account is properly connected before running."
      },
      {
        "type": "tip",
        "content": "You can use saved searches in Sales Navigator and copy their URLs as tasks."
      }
    ]
  },
  "seo": {
    "title": "Add Tasks - Sales Navigator Leads Scraper - lobstr.io API",
    "description": "Learn how to add LinkedIn Sales Navigator search URLs for lead scraping using the lobstr.io API."
  }
}

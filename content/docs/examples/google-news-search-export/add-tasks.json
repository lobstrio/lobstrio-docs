{
  "slug": "examples/google-news-search-export/add-tasks",
  "title": "Add Tasks",
  "description": "Add keyword-based tasks to scrape Google News articles",
  "category": "Google News Search Export",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/tasks",
    "requiresAuth": true
  },
  "content": {
    "introduction": "Add keyword-based tasks to scrape Google News articles. Each task represents a news search query that will fetch matching articles from Google News.",
    "sections": {
      "requestBody": {
        "title": "Request Body",
        "columns": [
          {"header": "Field", "key": "field"},
          {"header": "Type", "key": "type"},
          {"header": "Required", "key": "required"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"field": "**squid**", "type": "string", "required": "Yes", "description": "Hash of your squid configured with the google-news-search-export crawler"},
          {"field": "**tasks**", "type": "array", "required": "Yes", "description": "Array of task objects, each containing a `keyword` field"}
        ]
      },
      "taskFields": {
        "title": "Task Object",
        "columns": [
          {"header": "Field", "key": "field"},
          {"header": "Type", "key": "type"},
          {"header": "Required", "key": "required"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"field": "**keyword**", "type": "string", "required": "Yes", "description": "The news search query (e.g., `crab`, `climate change`, `tech news`)"}
        ]
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Request body format"
      }
    ],
    "proTips": [
      {
        "type": "tip",
        "content": "Use specific keywords for more targeted news results."
      },
      {
        "type": "note",
        "content": "Configure time range and language in the squid settings to filter results."
      }
    ]
  },
  "seo": {
    "title": "Add Tasks - Google News Search Export - lobstr.io API",
    "description": "Learn how to add keyword-based tasks to scrape Google News articles."
  }
}

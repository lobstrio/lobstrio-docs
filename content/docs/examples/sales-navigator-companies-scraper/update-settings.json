{
  "slug": "examples/sales-navigator-companies-scraper/update-settings",
  "title": "Update Settings",
  "description": "Configure the Sales Navigator Companies Scraper settings",
  "category": "Sales Navigator Companies Scraper",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/squids/{squid_hash}",
    "requiresAuth": true
  },
  "content": {
    "introduction": "Configure your Sales Navigator Companies Scraper squid settings. Control pagination depth and result limits.",
    "sections": {
      "scraperParams": {
        "title": "Scraper Parameters",
        "content": "Set these parameters in the `params` object:",
        "columns": [
          {"header": "Parameter", "key": "param"},
          {"header": "Type", "key": "type"},
          {"header": "Default", "key": "default"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"param": "**max_pages**", "type": "integer", "default": "100", "description": "Maximum number of search result pages to scrape (max: 100)"},
          {"param": "**max_results**", "type": "integer", "default": "unlimited", "description": "Maximum companies to collect per task (max: 5000)"},
          {"param": "**max_unique_results_per_run**", "type": "integer", "default": "unlimited", "description": "Maximum unique results across all tasks in the run"}
        ]
      },
      "squidSettings": {
        "title": "Squid Settings",
        "content": "Configure general squid settings:",
        "columns": [
          {"header": "Setting", "key": "setting"},
          {"header": "Type", "key": "type"},
          {"header": "Description", "key": "description"}
        ],
        "rows": [
          {"setting": "**name**", "type": "string", "description": "Display name for your squid configuration"},
          {"setting": "**concurrency**", "type": "integer", "description": "Number of parallel scraping threads (default: 1)"},
          {"setting": "**export_unique_results**", "type": "boolean", "description": "Export only unique results (deduplicated)"},
          {"setting": "**to_complete**", "type": "boolean", "description": "Run until all tasks complete"},
          {"setting": "**no_line_breaks**", "type": "boolean", "description": "Remove line breaks from results"}
        ]
      },
      "pagination": {
        "title": "Pagination",
        "content": "Sales Navigator displays 25 companies per page. With `max_pages` set to 100 (default), you can collect up to 2,500 companies per search URL.\n\nFor larger datasets, use multiple search URLs with different filters."
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Request body format"
      }
    ],
    "proTips": [
      {
        "type": "tip",
        "content": "Set `max_pages` to a lower value if you only need companies from the first few pages of results."
      },
      {
        "type": "note",
        "content": "Credits are charged per company collected (1 credit per company)."
      }
    ]
  },
  "seo": {
    "title": "Update Settings - Sales Navigator Companies Scraper - lobstr.io API",
    "description": "Learn how to configure Sales Navigator Companies Scraper settings using the lobstr.io API."
  }
}

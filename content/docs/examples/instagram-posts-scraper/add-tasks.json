{
  "slug": "examples/instagram-posts-scraper/add-tasks",
  "title": "Add Tasks",
  "description": "Add Instagram profile URLs, post URLs, or usernames to scrape posts using the Lobstr.io API",
  "category": "Instagram Posts Scraper",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/tasks",
    "requiresAuth": true
  },
  "content": {
    "introduction": "Add Instagram profiles or posts as tasks to your squid. The scraper will extract posts, captions, engagement metrics, and metadata from the provided profiles or individual posts.\n\nYou can provide a profile URL, a single post/reel URL, or just a username.",
    "sections": {
      "inputFormats": {
        "title": "Supported Input Formats",
        "content": "**Profile URL:**\n\n`https://www.instagram.com/natgeo/`\n\n`https://instagram.com/username`\n\n**Single Post URL:**\n\n`https://www.instagram.com/p/ABC123/`\n\n**Reel URL:**\n\n`https://www.instagram.com/reel/ABC123/`\n\n**Username only:**\n\n`natgeo`\n\n`cristiano`"
      },
      "requestBody": {
        "title": "Request Body",
        "columns": [
          { "header": "Field", "key": "field" },
          { "header": "Type", "key": "type" },
          { "header": "Required", "key": "required" },
          { "header": "Description", "key": "description" }
        ],
        "rows": [
          { "field": "**squid**", "type": "string", "required": "Yes", "description": "Hash of your squid configured with the Instagram Posts Scraper crawler" },
          { "field": "**tasks**", "type": "array", "required": "Yes", "description": "Array of task objects, each containing a `url` field" }
        ]
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Request body format"
      }
    ],
    "proTips": [
      {
        "type": "tip",
        "content": "You can use just the username without the full URL - the scraper will resolve it automatically."
      },
      {
        "type": "note",
        "content": "When scraping a profile, all public posts will be collected. For single posts, only that specific post data is returned."
      },
      {
        "type": "tip",
        "content": "You can add up to 1000 tasks in a single request for bulk operations."
      },
      {
        "type": "note",
        "content": "Duplicate URLs are automatically detected and won't be added twice."
      }
    ]
  },
  "seo": {
    "title": "Add Tasks - Instagram Posts Scraper - Lobstr.io API",
    "description": "Learn how to add Instagram profile URLs, post URLs, or usernames as tasks to extract posts and engagement data using the Lobstr.io API."
  }
}

{
  "slug": "sync-account",
  "title": "Sync Account",
  "description": "Synchronize cookies for a platform account to enable authenticated scraping",
  "category": "Account",
  "metadata": {
    "method": "POST",
    "endpoint": "/v1/accounts/cookies",
    "requiresAuth": true
  },
  "content": {
    "introduction": "This endpoint allows you to synchronize cookies for a specific platform account type. It accepts a JSON payload containing the account type and required cookie values, and returns a task ID to track the synchronization process.",
    "sections": {
      "bodyParameters": {
        "title": "Request Body",
        "parameters": [
          {
            "name": "type",
            "type": "string",
            "required": true,
            "description": "Account type identifier (e.g., twitter-sync, facebook-sync)",
            "example": "\"twitter-sync\""
          },
          {
            "name": "cookies",
            "type": "object",
            "required": true,
            "description": "Object containing required cookie name-value pairs",
            "example": "{\"auth_token\": \"...\", \"ct0\": \"...\"}"
          }
        ]
      },
      "responseFields": {
        "title": "Response Field Explanations",
        "fields": [
          {
            "path": "id",
            "type": "string",
            "description": "Synchronization task identifier",
            "example": "\"984e0a7996aeae956793b8967cd3e122\""
          },
          {
            "path": "object",
            "type": "string",
            "description": "Always \"synchronize-cookies\"",
            "example": "\"synchronize-cookies\""
          },
          {
            "path": "status_code",
            "type": "integer",
            "description": "Task status code (100 = created)",
            "example": "100"
          },
          {
            "path": "status_text",
            "type": "string",
            "description": "Human-readable status",
            "example": "\"created\""
          }
        ]
      }
    },
    "parameters": [],
    "headers": [
      {
        "key": "Authorization",
        "value": "Token YOUR_API_KEY",
        "required": true,
        "description": "Your API authentication token"
      },
      {
        "key": "Content-Type",
        "value": "application/json",
        "required": true,
        "description": "Must be application/json"
      }
    ],
    "proTips": [
      {
        "type": "tip",
        "content": "Use browser developer tools to extract cookies. Open DevTools → Application → Cookies, and copy the required cookie values."
      },
      {
        "type": "note",
        "content": "The returned task ID can be used with the Check Sync Status endpoint to monitor synchronization progress."
      },
      {
        "type": "warning",
        "content": "Ensure cookies are fresh and valid. Expired or invalid cookies will result in synchronization failure."
      },
      {
        "type": "tip",
        "content": "Use List Account Types endpoint first to discover which cookies are required for each platform."
      }
    ]
  },
  "seo": {
    "title": "Sync Account - lobstr.io API Documentation",
    "description": "Learn how to synchronize platform account cookies for authenticated scraping using the lobstr.io API."
  }
}
